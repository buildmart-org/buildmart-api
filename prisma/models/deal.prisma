model Deal {
    id          String   @id @default(uuid()) @db.Uuid
    title       String
    description String
    bannerTitle String
    bannerText  String

    endsAt     DateTime

    isActive   Boolean  @default(true)

    products   Product[]

    @@index([isActive])

    @@map("deals")
}

